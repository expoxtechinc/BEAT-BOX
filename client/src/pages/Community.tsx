import { useSupabaseAuth } from "@/contexts/SupabaseAuthContext";
import type { SocialPost } from "@/lib/models";
import { contentReferenceHref, FEED_PAGE_SIZE, getFeedRange, normalizeSocialPost, publicPublishedPosts } from "@/lib/feed";
import { supabase } from "@/lib/supabase";
import { ExternalLink, Image as ImageIcon, Loader2, Maximize2, MessageCircle, Package, Search, Send, Upload, UserPlus } from "lucide-react";
import { CommentThread } from "@/components/CommentThread";
import { SocialActions } from "@/components/SocialActions";
import { StoryRail } from "@/components/StoryRail";
import React, { useEffect, useState } from "react";
import { Link } from "wouter";

function contentTypePresentation(type: string) {
  switch (type) {
    case "movie": return { label: "Movie", mark: "FILM", action: "Watch details", copy: "Published movie release" };
    case "app": return { label: "App", mark: "APP", action: "View app", copy: "Published app release" };
    case "digital_product": return { label: "Digital product", mark: "SHOP", action: "View product", copy: "Published digital product" };
    case "software": return { label: "Software", mark: "CODE", action: "View software", copy: "Published software release" };
    case "video": return { label: "Video", mark: "VIDEO", action: "Watch details", copy: "Published video release" };
    default: return { label: "Beat", mark: "BEAT", action: "View beat", copy: "Published beat release" };
  }
}

export default function Community() {
  const { user, profile, loading } = useSupabaseAuth();
  const [posts, setPosts] = useState<SocialPost[]>([]); const [body, setBody] = useState(""); const [audience, setAudience] = useState<"public" | "friends" | "only_me">("public"); const [mediaFiles, setMediaFiles] = useState<File[]>([]); const [mediaUrls, setMediaUrls] = useState<Record<string, string>>({}); const [mediaErrors, setMediaErrors] = useState<Record<string, boolean>>({}); const [contentItems, setContentItems] = useState<Record<string, { id: string; title: string; content_type: string; price: number; currency: string; access_mode: string; description: string | null; slug: string }>>({}); const [lightboxUrl, setLightboxUrl] = useState<string | null>(null); const [busy, setBusy] = useState(false); const [loadingPosts, setLoadingPosts] = useState(true); const [feedError, setFeedError] = useState<string | null>(null); const [hasMore, setHasMore] = useState(false); const [feedPage, setFeedPage] = useState(0); const [message, setMessage] = useState<string | null>(null); const [liked, setLiked] = useState<Record<string, boolean>>({}); const [bookmarked, setBookmarked] = useState<Record<string, boolean>>({}); const [followed, setFollowed] = useState<Record<string, boolean>>({}); const [openComments, setOpenComments] = useState<Record<string, boolean>>({});
  const pageSize = FEED_PAGE_SIZE;
  const [composerOpen, setComposerOpen] = useState(false); const [searchQuery, setSearchQuery] = useState(""); const [suggestions, setSuggestions] = useState<Array<{ id: string; title: string; kind: string; href: string }>>([]); const [searching, setSearching] = useState(false); const [searchError, setSearchError] = useState<string | null>(null); const [discoveryItems, setDiscoveryItems] = useState<Array<{ id: string; title: string; content_type: string; description: string | null; slug: string }>>([]); const [discoveryError, setDiscoveryError] = useState<string | null>(null);
  const load = async (page = 0, append = false) => { setLoadingPosts(true); setFeedError(null); try { const [from, to] = getFeedRange(page, pageSize); const result = await supabase.from("social_posts").select("id,author_id,body,content_id,media_path,media_type,media_gallery,link_url,status,audience,like_count,comment_count,share_count,created_at,profiles!social_posts_author_id_fkey(display_name,avatar_url,username,professional_mode)").eq("status", "published").order("created_at", { ascending: false }).range(from, to); if (result.error) throw result.error; const normalized = publicPublishedPosts((result.data || []).map(row => normalizeSocialPost(row as SocialPost & { profiles?: SocialPost["profiles"] | SocialPost["profiles"][] }))); setPosts(current => append ? [...current, ...normalized] : normalized); setHasMore(normalized.length === pageSize); setFeedPage(page); const publicEntries = normalized.flatMap(post => { const gallery = post.media_gallery?.length ? post.media_gallery : post.media_path ? [{ path: post.media_path, type: post.media_type || "image" }] : []; return gallery.map((item, index) => [`${post.id}:${index}`, supabase.storage.from("social-media").getPublicUrl(item.path).data.publicUrl] as const); }); setMediaUrls(current => ({ ...(append ? current : {}), ...Object.fromEntries(publicEntries.filter(([, url]) => Boolean(url))) })); const referencedIds = normalized.map(post => post.content_id).filter((id): id is string => Boolean(id)); if (referencedIds.length) { const referenced = await supabase.from("content_items").select("id,title,content_type,price,currency,access_mode,description,slug").in("id", referencedIds).eq("status", "published"); if (referenced.error) throw referenced.error; setContentItems(current => ({ ...(append ? current : {}), ...Object.fromEntries((referenced.data || []).map(item => [item.id, item])) })); } else if (!append) { setContentItems({}); } if (user && !append) { const [likes, saves, follows] = await Promise.all([supabase.from("social_post_likes").select("post_id").eq("user_id", user.id), supabase.from("social_post_bookmarks").select("post_id").eq("user_id", user.id), supabase.from("producer_follows").select("producer_id").eq("follower_id", user.id)]); if (likes.error || saves.error || follows.error) throw likes.error || saves.error || follows.error; setLiked(Object.fromEntries((likes.data || []).map(row => [row.post_id, true]))); setBookmarked(Object.fromEntries((saves.data || []).map(row => [row.post_id, true]))); setFollowed(Object.fromEntries((follows.data || []).map(row => [row.producer_id, true]))); } } catch (error) { setFeedError(error instanceof Error ? error.message : "Unable to load the public Feed right now."); } finally { setLoadingPosts(false); } };
  useEffect(() => { void load(0, false); }, [user?.id]);
  useEffect(() => { void supabase.from("content_items").select("id,title,content_type,description,slug").eq("status", "published").order("created_at", { ascending: false }).limit(4).then(result => { if (result.error) setDiscoveryError("Discovery is temporarily unavailable."); else setDiscoveryItems(result.data || []); }); }, []);
  const requestedPostId = new URLSearchParams(window.location.search).get("post");
  useEffect(() => { if (!requestedPostId || !posts.length) return; const target = document.getElementById(`feed-post-${requestedPostId}`); target?.scrollIntoView({ behavior: "smooth", block: "center" }); target?.classList.add("is-deep-linked"); const timer = window.setTimeout(() => target?.classList.remove("is-deep-linked"), 1800); return () => window.clearTimeout(timer); }, [posts, requestedPostId]);
  useEffect(() => { const query = searchQuery.trim(); if (query.length < 2) { setSuggestions([]); setSearchError(null); setSearching(false); return; } const timer = window.setTimeout(async () => { setSearching(true); setSearchError(null); try { const [items, creators, feedPosts] = await Promise.all([supabase.from("content_items").select("id,title,content_type").eq("status", "published").ilike("title", `%${query}%`).order("created_at", { ascending: false }).limit(4), supabase.from("profiles").select("id,display_name,username").eq("account_status", "active").or(`display_name.ilike.%${query}%,username.ilike.%${query}%`).limit(3), supabase.from("social_posts").select("id,body").eq("status", "published").ilike("body", `%${query}%`).order("created_at", { ascending: false }).limit(3)]); const failure = items.error || creators.error || feedPosts.error; if (failure) throw failure; const next = [...(items.data || []).map(item => ({ id: item.id, title: item.title, kind: item.content_type, href: contentReferenceHref(item.id) })), ...(creators.data || []).map(creator => ({ id: creator.id, title: creator.display_name || creator.username || "BeatBox creator", kind: "creator", href: `/creators/${creator.username || creator.id}` })), ...(feedPosts.data || []).map(post => ({ id: post.id, title: post.body?.slice(0, 70) || "Community post", kind: "post", href: `/feed?post=${post.id}` }))]; setSuggestions(next); } catch (error) { setSuggestions([]); setSearchError(error instanceof Error ? error.message : "Search is temporarily unavailable."); } finally { setSearching(false); } }, 250); return () => window.clearTimeout(timer); }, [searchQuery]);
  const publish = async (event: React.FormEvent) => { event.preventDefault(); if (!user || (!body.trim() && !mediaFiles.length)) return; if (mediaFiles.length > 8 || mediaFiles.some(file => file.size > 50 * 1024 * 1024)) { setMessage("Choose up to 8 public media files, each 50 MB or smaller."); return; } setBusy(true); setMessage(null); try { const gallery = await Promise.all(mediaFiles.map(async file => { const type: "image" | "audio" | "video" = file.type.startsWith("image/") ? "image" : file.type.startsWith("video/") ? "video" : "audio"; const path = `${user.id}/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g, "-")}`; const { error: uploadError } = await supabase.storage.from("social-media").upload(path, file, { upsert: false, contentType: file.type }); if (uploadError) throw uploadError; return { path, type }; })); const first = gallery[0] || null; const { error } = await supabase.from("social_posts").insert({ author_id: user.id, body: body.trim() || null, media_path: first?.path || null, media_type: first?.type || null, media_gallery: gallery, audience, status: "published" }); if (error) throw error; setMessage("Post published to the BeatBox community."); setComposerOpen(false); setBody(""); setMediaFiles([]); setAudience("public"); void load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to publish post."); } finally { setBusy(false); } };
  const toggleLike = async (post: SocialPost) => { if (!user) { setMessage("Sign in to react to community posts."); return; } const exists = liked[post.id]; const nextLiked = !exists; setLiked(current => ({ ...current, [post.id]: nextLiked })); setPosts(current => current.map(item => item.id === post.id ? { ...item, like_count: Math.max(0, item.like_count + (exists ? -1 : 1)) } : item)); const result = exists ? await supabase.from("social_post_likes").delete().eq("post_id", post.id).eq("user_id", user.id) : await supabase.from("social_post_likes").insert({ post_id: post.id, user_id: user.id }); if (result.error) { setLiked(current => ({ ...current, [post.id]: exists })); setPosts(current => current.map(item => item.id === post.id ? { ...item, like_count: Math.max(0, item.like_count + (exists ? 1 : -1)) } : item)); setMessage(result.error.message); } };
  const toggleBookmark = async (post: SocialPost) => { if (!user) { setMessage("Sign in to save posts."); return; } const exists = bookmarked[post.id]; const nextBookmarked = !exists; setBookmarked(current => ({ ...current, [post.id]: nextBookmarked })); const result = exists ? await supabase.from("social_post_bookmarks").delete().eq("post_id", post.id).eq("user_id", user.id) : await supabase.from("social_post_bookmarks").insert({ post_id: post.id, user_id: user.id }); if (result.error) { setBookmarked(current => ({ ...current, [post.id]: exists })); setMessage(result.error.message); } };
  const comment = async (post: SocialPost) => { if (!user) { setMessage("Sign in to comment."); return; } const text = window.prompt("Add a comment to this post"); if (!text?.trim()) return; const { error } = await supabase.from("social_post_comments").insert({ post_id: post.id, user_id: user.id, body: text.trim() }); if (error) setMessage(error.message); else setMessage("Comment added."); };
  const repost = async (post: SocialPost) => { if (!user) { setMessage("Sign in to repost."); return; } const { error } = await supabase.from("social_reposts").insert({ post_id: post.id, user_id: user.id }); setMessage(error ? error.message : "Repost saved to your activity."); };
  const toggleFollow = async (authorId: string) => { if (!user || authorId === user.id) return; const exists = followed[authorId]; const result = exists ? await supabase.from("producer_follows").delete().eq("follower_id", user.id).eq("producer_id", authorId) : await supabase.from("producer_follows").insert({ follower_id: user.id, producer_id: authorId }); if (!result.error) { setFollowed({ ...followed, [authorId]: !exists }); setMessage(exists ? "Unfollowed creator." : "Following creator."); } };
  const friendAction = async (authorId: string) => { if (!user || authorId === user.id) return; const [outgoing, incoming] = await Promise.all([supabase.from("social_friend_requests").select("requester_id,addressee_id,status").eq("requester_id", user.id).eq("addressee_id", authorId).maybeSingle(), supabase.from("social_friend_requests").select("requester_id,addressee_id,status").eq("requester_id", authorId).eq("addressee_id", user.id).maybeSingle()]); if (outgoing.error || incoming.error) { setMessage((outgoing.error || incoming.error)?.message || "Unable to check friendship."); return; } if (incoming.data?.status === "pending") { const { error } = await supabase.from("social_friend_requests").update({ status: "accepted" }).eq("requester_id", authorId).eq("addressee_id", user.id); setMessage(error ? error.message : "Friend request confirmed."); return; } if (outgoing.data?.status === "accepted" || incoming.data?.status === "accepted") { const { error } = await supabase.from("social_friend_requests").delete().or(`and(requester_id.eq.${user.id},addressee_id.eq.${authorId}),and(requester_id.eq.${authorId},addressee_id.eq.${user.id})`); setMessage(error ? error.message : "Friend removed."); return; } const { error } = await supabase.from("social_friend_requests").upsert({ requester_id: user.id, addressee_id: authorId, status: "pending" }, { onConflict: "requester_id,addressee_id" }); setMessage(error ? error.message : "Friend request sent."); };
  const moderate = async (authorId: string, kind: "block" | "mute") => { if (!user || authorId === user.id) return; const result = kind === "block" ? await supabase.from("social_blocks").upsert({ blocker_id: user.id, blocked_id: authorId }) : await supabase.from("social_mutes").upsert({ muter_id: user.id, muted_id: authorId }); setMessage(result.error ? result.error.message : `${kind === "block" ? "Creator blocked" : "Creator muted"}.`); if (!result.error && kind === "block") setPosts(posts.filter(post => post.author_id !== authorId)); };
  const removePost = async (post: SocialPost) => { if (!user || post.author_id !== user.id) return; if (!window.confirm("Delete this post permanently?")) return; const { error } = await supabase.from("social_posts").delete().eq("id", post.id).eq("author_id", user.id); if (error) setMessage(error.message); else { setPosts(current => current.filter(item => item.id !== post.id)); setMessage("Post deleted."); } };
  const report = async (post: SocialPost) => { if (!user) { setMessage("Sign in to report content."); return; } const reason = window.prompt("Why are you reporting this post?"); if (!reason?.trim()) return; const { error } = await supabase.from("reports").insert({ reporter_id: user.id, reason: reason.trim(), reported_post_id: post.id, description: "Community post report" }); setMessage(error ? error.message : "Report submitted for moderation."); };
  if (loading) return <section className="status-page"><Loader2 className="spin" /><p>Loading community…</p></section>;
  const lightbox = lightboxUrl ? <div className="feed-lightbox" role="dialog" aria-modal="true" aria-label="Community image viewer" onClick={() => setLightboxUrl(null)}><button type="button" className="feed-lightbox__close" onClick={() => setLightboxUrl(null)}>Close</button><img src={lightboxUrl} alt="Expanded community attachment" onClick={event => event.stopPropagation()} /></div> : null;
  return <section className="dashboard-page community-page"><div className="container"><StoryRail /><div className="dashboard-title"><div><p className="eyebrow"><span /> BeatBox community</p><h1>Build in public. Move together.</h1></div>{user ? <Link className="button button--small" href="/studio"><UserPlus size={15} /> Open studio</Link> : <Link className="button button--small" href="/auth">Join the community</Link>}</div><div className="feed-search" role="search"><label htmlFor="feed-marketplace-search"><Search size={16} /> Search marketplace beats</label><input id="feed-marketplace-search" className="input" value={searchQuery} onChange={event => setSearchQuery(event.target.value)} placeholder="Search published beats and creator content" autoComplete="off" aria-expanded={suggestions.length > 0} />{(searching || suggestions.length > 0 || searchError || (searchQuery.trim().length >= 2 && !searching)) && <div className="feed-search__suggestions" role="listbox">{searching ? <span className="feed-search__status">Searching…</span> : searchError ? <span className="feed-search__status form-error">{searchError}</span> : suggestions.length ? suggestions.map(item => <Link key={`${item.kind}-${item.id}`} href={item.href} role="option" onClick={() => { setSearchQuery(""); setSuggestions([]); }}><b>{item.title}</b><small>{item.kind}</small></Link>) : <span className="feed-search__status">No matching creators, posts, or published marketplace items.</span>}</div>}</div><section className="dashboard-panel feed-discovery" aria-labelledby="feed-discovery-title"><div className="dashboard-title"><div><p className="eyebrow"><span /> Discover</p><h2 id="feed-discovery-title">Fresh from BeatBox creators</h2></div><Link className="text-button" href="/catalog">Browse all</Link></div>{discoveryError ? <p className="feed-search__status form-error" role="alert">{discoveryError}</p> : discoveryItems.length ? <div className="feed-discovery__grid">{discoveryItems.map(item => { const presentation = contentTypePresentation(item.content_type); return <Link key={item.id} className={`feed-content-card feed-content-card--${item.content_type}`} data-content-type={item.content_type} href={contentReferenceHref(item.id)}><div><span className="feed-content-card__type-mark" aria-hidden="true">{presentation.mark}</span><span className="eyebrow"><Package size={13} /> {presentation.label}</span><h3>{item.title}</h3><small>{presentation.copy}</small>{item.description && <p>{item.description}</p>}</div><span className="content-link">{presentation.action} <ExternalLink size={15} /></span></Link>; })}</div> : <p className="feed-search__status">Published creator drops will appear here.</p>}</section><button type="button" className="button button--small composer-sheet-trigger" onClick={() => setComposerOpen(true)}><Send size={15} /> Create post</button><form className={`dashboard-panel community-composer ${composerOpen ? "is-open" : ""}`} onSubmit={publish}><div className="community-composer__heading"><h2>Share an update</h2><button type="button" className="text-button community-composer__close" onClick={() => setComposerOpen(false)}>Close</button></div><textarea rows={3} maxLength={3000} placeholder={user ? `What are you creating, ${profile?.display_name || "creator"}?` : "Sign in to share with the community."} value={body} onChange={event => setBody(event.target.value)} disabled={!user} /><label className="feed-composer__privacy" htmlFor="post-audience"><span>Who can see this?</span><select id="post-audience" value={audience} onChange={event => setAudience(event.target.value as "public" | "friends" | "only_me")} disabled={!user}><option value="public">Public</option><option value="friends">Friends</option><option value="only_me">Only me</option></select></label><label className="file-input file-input--compact"><Upload size={16} /><b>{mediaFiles.length ? `${mediaFiles.length} public attachment${mediaFiles.length === 1 ? "" : "s"} selected` : "Attach images, audio, or video"}</b><input type="file" multiple accept="image/jpeg,image/png,image/webp,audio/*,video/mp4,video/webm" onChange={event => setMediaFiles(Array.from(event.target.files || []).slice(0, 8))} disabled={!user} /></label><div><small>{body.length}/3000 · {audience === "public" ? "Public post · Anyone can see this" : audience === "friends" ? "Friends only · mutual accepted friends can see this" : "Only you can see this"}</small><button className="button button--small" disabled={!user || busy || (!body.trim() && !mediaFiles.length)}>{busy ? <Loader2 className="spin" size={15} /> : <Send size={15} />} Publish</button></div>{message && <p className={message.includes("error") || message.includes("Sign in") ? "form-error" : "form-success"}>{message}</p>}</form>{feedError && <div className="form-error feed-error" role="alert">{feedError} <button className="text-button" type="button" onClick={() => void load(0, false)}>Retry</button></div>}{loadingPosts && !posts.length ? <div className="empty-featured empty-featured--light"><Loader2 className="spin" size={34} /><h2>Loading the BeatBox Feed…</h2><p>Checking published updates and secure media links.</p></div> : <><div className="community-feed">{posts.length ? posts.map(post => <article id={`feed-post-${post.id}`} className="dashboard-panel community-post" key={post.id}><div className="community-post__header"><div className="avatar-placeholder">{(post.profiles?.display_name || "B").slice(0, 1).toUpperCase()}</div><div><b>{post.profiles?.display_name || "BeatBox creator"}</b><small>{new Date(post.created_at).toLocaleString()}</small></div>{user && user.id !== post.author_id && <div className="community-post__author-actions"><button className="text-button" onClick={() => void toggleFollow(post.author_id)}>{followed[post.author_id] ? "Following" : "Follow"}</button><button className="text-button" onClick={() => void friendAction(post.author_id)}>Add / confirm friend</button><button className="text-button" onClick={() => void moderate(post.author_id, "mute")}>Mute</button><button className="text-button" onClick={() => void moderate(post.author_id, "block")}>Block</button></div>}</div>{post.body && <p className="community-post__body">{post.body}</p>}{post.link_url && <a className="content-link community-post__link" href={post.link_url} target="_blank" rel="noreferrer"><ExternalLink size={15} /> Open shared link</a>}{(post.media_gallery?.length || post.media_path) && <div className="community-post__media community-post__media--gallery">{(post.media_gallery?.length ? post.media_gallery : [{ path: post.media_path!, type: post.media_type || "image" }]).map((item, index) => { const url = mediaUrls[`${post.id}:${index}`] || mediaUrls[post.id]; return url ? <div className="community-post__media-item" key={`${post.id}-${index}`}>{item.type === "image" ? <button type="button" className="community-post__image-button" onClick={() => setLightboxUrl(url)} aria-label="Open community image"><img loading="lazy" src={url} alt={`Community attachment ${index + 1}`} onError={() => setMediaErrors(current => ({ ...current, [`${post.id}:${index}`]: true }))} /><span><Maximize2 size={15} /> View image</span></button> : item.type === "video" ? <video controls preload="metadata" src={url} onError={() => setMediaErrors(current => ({ ...current, [`${post.id}:${index}`]: true }))} /> : <audio controls preload="metadata" src={url} onError={() => setMediaErrors(current => ({ ...current, [`${post.id}:${index}`]: true }))} />}{mediaErrors[`${post.id}:${index}`] && <small className="form-error">This public media could not be loaded.</small>}</div> : null; })}</div>}{post.content_id && (contentItems[post.content_id] ? <div className={`feed-content-card feed-content-card--${contentItems[post.content_id].content_type}`} data-content-type={contentItems[post.content_id].content_type}><div><span className="feed-content-card__type-mark" aria-hidden="true">{contentTypePresentation(contentItems[post.content_id].content_type).mark}</span><span className="eyebrow"><Package size={13} /> {contentTypePresentation(contentItems[post.content_id].content_type).label}</span><h3>{contentItems[post.content_id].title}</h3><small>{contentTypePresentation(contentItems[post.content_id].content_type).copy} · {contentItems[post.content_id].access_mode === "paid_download" ? `${contentItems[post.content_id].currency} ${contentItems[post.content_id].price}` : "Free or stream access"}</small>{contentItems[post.content_id].description && <p>{contentItems[post.content_id].description}</p>}</div><Link href={contentReferenceHref(post.content_id)} className="content-link"><ExternalLink size={15} /> {contentTypePresentation(contentItems[post.content_id].content_type).action}</Link></div> : <Link href={contentReferenceHref(post.content_id)} className="content-link"><ExternalLink size={15} /> View attached creator content</Link>)}<SocialActions postId={post.id} userId={user?.id} liked={Boolean(liked[post.id])} bookmarked={Boolean(bookmarked[post.id])} likeCount={post.like_count} commentCount={post.comment_count} shareCount={post.share_count} onLike={() => void toggleLike(post)} onSave={() => void toggleBookmark(post)} onComment={() => setOpenComments(current => ({ ...current, [post.id]: !current[post.id] }))} onRepost={() => void repost(post)} onReport={() => void report(post)} onDelete={user?.id === post.author_id ? () => void removePost(post) : undefined} audienceLabel={post.audience === "friends" ? "Friends" : post.audience === "only_me" ? "Only me" : "Public"} onShared={() => setMessage("Share recorded.")} />{openComments[post.id] && <CommentThread postId={post.id} userId={user?.id} onCountChange={() => undefined} />}</article>) : <div className="empty-featured empty-featured--light"><MessageCircle size={34} /><h2>The feed is ready for its first update.</h2><p>Share a launch, a new beat, a product, or a collaboration request.</p></div>}</div>{hasMore && <div className="feed-pagination"><button className="button button--ghost" type="button" onClick={() => void load(feedPage + 1, true)} disabled={loadingPosts}>{loadingPosts ? <Loader2 className="spin" size={15} /> : null}{loadingPosts ? "Loading more…" : "Load more"}</button></div>}</>}</div>{lightbox}</section>;
}
